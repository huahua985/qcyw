<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qcyw.xchqcyws.mapper.UserMapper">

    <insert id="insertUser" parameterType="String">
        insert into sys_user (id,openid,unionid,phone)
        values (#{id},#{openid},#{unionid},#{phone})
    </insert>

    <update id="updateUserWxInfo">
        update sys_user
        set nick_name = #{nickName}
        where openid = #{openid}
    </update>

    <update id="updateUserInfo" >
        update sys_user
        set phone = #{userVo.phone}
        where openid = #{openid}
    </update>

    <select id="queryUserInfo" parameterType="String" resultType="user">
        select  sys_user.openid ,sys_user.nick_name, sys_user.avatar,
        sys_user.phone
        from sys_user
        where openid = #{openid}
    </select>
</mapper>
